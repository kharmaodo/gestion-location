# Contribution

1. Partir de `develop`.
2. Créer `feat/<jalon>-<sujet>` (ex. `feat/j1-biens-crud`).
3. Ouvrir une PR vers `develop`.
4. Merger uniquement après validation explicite du PO.

`develop` et `main` ne reçoivent pas de push direct de feature.
